// すとぷり10th 管理画面レイアウト（ゴールド系）。公開サイトのテーマとは分離。
export default function Admin10thLayout({ children }: { children: React.ReactNode }) {
  return <div className="admin-10th min-h-screen bg-gold-50/40 font-serif">{children}</div>
}
